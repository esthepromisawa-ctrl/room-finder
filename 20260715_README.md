# 会議室 空き状況ファインダー（Web公開版）

作成日：2026-07-15

りざぶ郎の会議室予約状況を表示し、空き枠検索・予約（りざぶ郎への事前入力＋Googleカレンダー同時登録）ができる社内向けWebツールです。GitHub Pagesで公開し、GitHub Actionsが定期的にデータを更新するため、特定のPCを起動しておく必要はありません。

## 仕組み（在庫ダッシュボードと同方式）

- `scripts/fetch-board.mjs`：りざぶ郎から今日以降21日分の予約を取得し `board.json` を生成
- `.github/workflows/update-board.yml`：15分おき（JST日中）に上記を実行し `board.json` を自動更新・push
- `index.html`：`board.json` を読み込み、タイムテーブル表示・空き枠検索・予約リンク生成を行う静的ページ（GitHub Pagesが配信）

## 予約の流れ

- 空き枠をクリック → 時間を調整 → 「開く」
  - りざぶ郎の予約画面が部屋・日時入りで開くので、内容を確認して「OK」で確定します
  - 同時にGoogleカレンダーの登録画面も開けます（チェックで選択）
- 予約の削除・変更はこのサイトではできません。りざぶ郎から直接おこなってください

## ローカルでの確認方法

- `node scripts/fetch-board.mjs` で `board.json` を生成
- このフォルダで簡易サーバーを起動（例：`python -m http.server 5327`）し、`http://localhost:5327` を開く

## 注意事項

- りざぶ郎の非公式な仕組みを利用しているため、先方の仕様変更で動かなくなる可能性があります
- データは「最後の自動更新時点」のものです（15分ごと）。最終的な予約可否はりざぶ郎の画面で確定時に判定されます
- 公開リポジトリの場合、`board.json`（予約者名・件名を含む）がインターネット上で閲覧可能になります。取り扱いにご注意ください

更新日：2026-07-15
